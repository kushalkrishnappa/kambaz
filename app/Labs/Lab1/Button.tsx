"use client";

export default function Button() {
  return (
    <button
      type="button"
      onClick={() => alert("Life is Good!")}
      id="wd-all-good"
    >
      Hello World!
    </button>
  );
}